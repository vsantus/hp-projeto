import styled from "styled-components";

export const ParagrafoStyled = styled.p`
  font-weight: 500;
  color: var(--amarelo-mostarda);
  margin: 0;

  

  span {
    color: white;
    margin-left: 6px;
  }
`;